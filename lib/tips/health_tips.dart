import 'package:flutter/material.dart';

class HealthTips extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tips For Good Health'),
      ),
    );
  }
}
