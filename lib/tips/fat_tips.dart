import 'package:flutter/material.dart';

class FatTips extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tips for Over-weight'),
      ),
    );
  }
}
